interface:
  display_name: "Micro-world"
  short_description: "Simulate a concept and learn by poking"
policy:
  # Codex's half of the SKILL.md `disable-model-invocation: true`. Keep both in
  # sync: without this, Codex still fires the skill implicitly. `$micro-world`
  # keeps working either way.
  allow_implicit_invocation: false
