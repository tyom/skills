# No `policy.allow_implicit_invocation` here: this skill is model-invoked in
# both harnesses, and the key defaults to true. Set it to false only alongside
# `disable-model-invocation: true` in SKILL.md.
interface:
  display_name: "Humanizer"
  short_description: "Rewrite prose so it reads like a person wrote it"
