interface:
  display_name: "Explainer"
  short_description: "Build an interactive HTML explainer"
policy:
  # Codex's half of the SKILL.md `disable-model-invocation: true`. Keep both in
  # sync: without this, Codex still fires the skill implicitly. `$explainer`
  # keeps working either way.
  allow_implicit_invocation: false
